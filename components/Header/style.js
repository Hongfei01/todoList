import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  img: {
    width: 170,
  },
  subtitle: {
    marginTop: -20,
    fontSize: 20,
    color: '#ABABAB',
  },
});

export default style;
